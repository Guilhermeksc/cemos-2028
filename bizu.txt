https://github.com/Guilhermeksc/360_novo/blob/mainnovo/src/modules/dispensa_eletronica/models.py


docker compose up -d --build

docker compose exec backend python manage.py makemigrations
docker compose exec backend python manage.py migrate
docker compose exec backend python manage.py check


crie um component para um layout de visualização dos Conceitos em frontend\src\app\interfaces\informacoes.interface.ts

export interface Conceitos {
  id: number;
  bibliografia: number;
  bibliografia_titulo?: string;
  titulo: string;
  descricao?: string;
  caiu_em_prova: boolean;
  ano_prova?: number;
}

frontend\src\app\interfaces\informacoes.interface.ts


a ideia é que esse component seja reutilizável em outros components

deverá ser uma relação título e descrição