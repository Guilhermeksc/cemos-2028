name: 🚀 Deploy to Hostinger Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🔑 Configurar acesso SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🧩 Adicionar host ao known_hosts
        run: |
          ssh-keyscan -H 195.200.1.112 >> ~/.ssh/known_hosts

      - name: 🚀 Executar comandos no servidor
        run: |
          ssh root@195.200.1.112 << 'EOF'
            cd ~/cemos-2028
            echo "📦 Atualizando repositório..."
            git pull origin master

            echo "🏗️ Build do CEMOS..."
            cd frontend-cemos
            npm install --legacy-peer-deps
            npm run build:prod

            echo "🏗️ Build do Auditapro..."
            cd ../frontend-audita
            npm install --legacy-peer-deps
            npm run build:prod

            echo "🔁 Reiniciando containers Docker..."
            cd ..
            docker compose restart backend nginx

            echo "✅ Deploy concluído com sucesso!"
          EOF
